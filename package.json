{
  "name": "nemv",
  "version": "0.0.1",
  "scripts": {
    "dev": "NODE_ENV=development node ./be/bin/www",
    "serve": "cd fe && yarn serve --fix",
    "prod": "cd fe && yarn && yarn build --fix && cd ../be && yarn && cd .. && NODE_ENV=production PORT=80 node ./be/bin/www"
  },
  "dependencies": {}
}
